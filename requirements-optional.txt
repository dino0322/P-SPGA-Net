# Optional dependencies for legacy comparison models.
# The core A5/P-SPGA ablation path does not require all of these.
timm>=0.9
einops>=0.7
transformers>=4.40
tensorflow>=2.12
mamba-ssm
# MinkowskiEngine is only needed by sparse ConvNeXt/FCMAE files and usually
# requires a CUDA-specific installation command.

